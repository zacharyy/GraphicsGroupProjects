#ifndef OBJECT_H
#define OBJECT_H

#include <vector>
#include "graphics_headers.h"

class Object
{
  public:
    Object();
    ~Object();
    void Update(unsigned int dt, bool spin_reverse, bool spin_stop, bool revolution_reverse, bool revolution_stop);
    void Render();

    glm::mat4 GetModel();

  private:
    glm::mat4 model;
    std::vector<Vertex> Vertices;
    std::vector<unsigned int> Indices;
    GLuint VB;
    GLuint IB;

    //float angle;
    float spin_angle; //spin
    float rev_angle; //revolution
};

#endif /* OBJECT_H */
